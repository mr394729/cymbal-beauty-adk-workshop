# Quickstart fixtures

`make_evalsets.py` maintains the example evaluation sets; `make_promo_proof.py` creates the promotion-proof document used by quickstart 07. These support the [quickstart catalog](../README.md). The catalog checks generated evaluation-set freshness.
