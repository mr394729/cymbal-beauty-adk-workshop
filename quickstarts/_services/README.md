# Local example services

The [orders_api](orders_api/app.py) FastAPI application is the mock order service used by [quickstart 04](../04-external-api-agent/README.md). Start it with the command in that guide. Its purpose is to demonstrate an external operational API boundary, independently of the main tablet service.
