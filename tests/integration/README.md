# Integration checks

[All test suites](../README.md)

These live smoke checks invoke the agent against cloud services. Run `uv run pytest tests/integration -q -m live` after setup; they are separate from the offline unit suite.
