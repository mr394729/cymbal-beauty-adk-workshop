# Quickstarts checks

[All test suites](../README.md)

Catalog checks launch each standalone example’s own tests in a separate process. Run `uv run pytest tests/quickstarts -q`; no model call or cloud service is required.
