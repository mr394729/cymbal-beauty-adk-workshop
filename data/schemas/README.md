# BigQuery schemas

The JSON files define the thirteen generated tables. Use [the data guide](../README.md) and [notebook 01](../../notebooks/01_store_data.ipynb) for relationships and sample records. Change the generator, backend contracts and affected evaluations together when changing a schema.
