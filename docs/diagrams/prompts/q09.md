# q09

**Slide:** none (quickstart 09 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/09-guardrails-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A risky request', smaller lines: 'contact details in the text', 'an HR question, an injection attempt'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'Guards in code, not in the prompt', smaller lines: 'before the model: redact and pseudonymise', 'before a tool: check role and store', 'plugins record what was blocked'. Inside the box at the bottom, small White chips with a Blue border in a row: 'before_model_callback', 'before_tool_callback', 'plugins'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'A safe answer', smaller lines: 'recommendation only for HR', 'the refusal is tested, not hoped for'.

The first arrow is labelled 'sends' and the second 'receives'.

BOTTOM, one thin Gray strip across the full width: 'the guardrails that must always hold live in callbacks and plugins'.

Exactly three boxes, 3 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A high-resolution 16:9 widescreen technical architecture diagram in a clean, modern flat vector graphic style on a solid White background. The composition consists of exactly three main rounded rectangular stages arranged horizontally in a row from left to right, connected by exactly two thick Charcoal directional arrows, with exactly one thin horizontal strip centered along the bottom. No title, no subtitle, no header, and no footer exist on the canvas.

On the left, a rounded rectangular box features a light purple fill with a solid Purple border. At the top-left inside this box sits a minimalist flat vector person silhouette icon in Purple. Centered horizontally within the box is bold Charcoal text reading "A risky request", followed directly below by two separate lines of regular Charcoal text: "contact details in the text" on the first line, and "an HR question, an injection attempt" on the second line.

Connecting the left box to the middle box is a thick horizontal Charcoal arrow pointing directly to the right. Directly above this arrow is horizontal Charcoal text reading "sends".

In the center, a larger rounded rectangular box features a light blue fill with a solid Blue border. Near the top of the box is bold Charcoal text reading "Guards in code, not in the prompt". Directly below this are three separate lines of regular Charcoal text: "before the model: redact and pseudonymise", followed by "before a tool: check role and store", followed by "plugins record what was blocked". Positioned horizontally side by side inside the bottom area of this middle box are exactly three small rounded rectangular chips, each featuring a solid White fill, a thin Blue border, and horizontal Charcoal monospace text on a single unbroken line: the first chip reads "before_model_callback", the middle chip reads "before_tool_callback", and the third chip reads "plugins".

Connecting the middle box to the right box is a second thick horizontal Charcoal arrow pointing directly to the right. Directly above this arrow is horizontal Charcoal text reading "receives".

On the right, a rounded rectangular box features a light green fill with a solid Green border. Centered horizontally within the box is bold Charcoal text reading "A safe answer", followed directly below by two separate lines of regular Charcoal text: "recommendation only for HR" on the first line, and "the refusal is tested, not hoped for" on the second line.

Positioned horizontally beneath all three boxes, spanning across the width of the canvas with generous side margins, is a single thin horizontal strip with a light Gray fill and a subtle Gray outline. Centered inside this strip is horizontal Charcoal text reading "the guardrails that must always hold live in callbacks and plugins".

All typography uses a clean geometric sans-serif font with high contrast, large enough to remain crisp and fully legible when scaled down. All text is strictly horizontal. Exactly three boxes, three chips, two arrows, and one strip appear on the canvas. No additional text, logos, drop shadows, gradients, or 3D effects are present.
