# q01

**Slide:** none (quickstart 01 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/01-hello-tool-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A question about stock', smaller lines: 'a product and a store', 'typed by the person'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'One agent, one tool', smaller lines: 'the model decides to call the tool', 'the tool reads the store records', 'the answer quotes the facts'. Inside the box at the bottom, small White chips with a Blue border in a row: 'LlmAgent', 'check_store_stock', 'App'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'An answer from records', smaller lines: 'shelf and backroom quantities', 'an error stays visible'.

The first arrow is labelled 'asks' and the second 'answers'.

BOTTOM, one thin Gray strip across the full width: 'the smallest useful ADK agent: one LlmAgent, one function tool, one App'.

Exactly three boxes, 3 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A clean, modern 2D technical architecture diagram in 16:9 widescreen format on a pure solid white background. The overall aesthetic is flat, clean, minimalist developer documentation styling with no 3D effects, no gradients, no drop shadows, and no decorative clutter. There is no diagram title, no subtitle, and no footer.

The layout consists of three primary rounded rectangular boxes arranged horizontally in a straight line from left to right, connected by two thick Charcoal right-pointing arrows, with one thin horizontal strip centered along the bottom.

1. Left box:
   - Rounded rectangle with a light purple fill and a distinct solid Purple border.
   - At the top left inside this box, a small, simple minimalist person silhouette icon in Purple.
   - Large bold Charcoal text centered inside: "A question about stock"
   - Directly underneath in smaller regular Charcoal text:
     "a product and a store"
     "typed by the person"

2. First connecting arrow:
   - A thick, solid Charcoal arrow pointing horizontally from the right edge of the left box to the left edge of the middle box.
   - Labeled clearly above the arrow in horizontal Charcoal text: "asks"

3. Middle box:
   - A slightly larger rounded rectangle with a light blue fill and a distinct solid Blue border.
   - Large bold Charcoal text centered near the top: "One agent, one tool"
   - Directly underneath in smaller regular Charcoal text, stacked vertically:
     "the model decides to call the tool"
     "the tool reads the store records"
     "the answer quotes the facts"
   - Positioned horizontally in a row near the bottom inside this box are three small white rounded pill-shaped chips, each with a solid Blue border and crisp Charcoal text:
     - Left chip: "LlmAgent"
     - Middle chip: "check_store_stock" (in clean monospace lettering, on a single unbroken line)
     - Right chip: "App"

4. Second connecting arrow:
   - A thick, solid Charcoal arrow pointing horizontally from the right edge of the middle box to the left edge of the right box.
   - Labeled clearly above the arrow in horizontal Charcoal text: "answers"

5. Right box:
   - Rounded rectangle with a light green fill and a distinct solid Green border.
   - Large bold Charcoal text centered inside: "An answer from records"
   - Directly underneath in smaller regular Charcoal text:
     "shelf and backroom quantities"
     "an error stays visible"

6. Bottom strip:
   - Centered horizontally underneath all three boxes and spanning nearly the full width of the canvas, a thin, elegant horizontal rounded strip with a light Gray fill and a subtle Gray border.
   - Centered horizontally inside the strip in medium regular Charcoal text:
     "the smallest useful ADK agent: one LlmAgent, one function tool, one App"

All text is strictly horizontal, perfectly legible, with generous white space and clean padding around all elements. There are no other boxes, icons, arrows, lines, logos, or text on the canvas.
