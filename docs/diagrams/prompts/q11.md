# q11

**Slide:** none (quickstart 11 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/11-ambient-event-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A store exception event', smaller lines: 'an on-shelf availability exception', 'product, store, observation time'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'An agent with no chat', smaller lines: 'reads the stock position', 'forms one supported recommendation', 'structured output, not prose'. Inside the box at the bottom, small White chips with a Blue border in a row: 'Pub/Sub toolset', 'output_schema'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'A published recommendation', smaller lines: 'on a Pub/Sub topic for a person to review', 'no task is created'.

The first arrow is labelled 'event in' and the second 'recommendation out'.

BOTTOM, one thin Gray strip across the full width: 'event in, structured recommendation out; the store manager decides'.

Exactly three boxes, 2 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A 16:9 technical architecture pipeline diagram on a pure solid white background, designed with clean 2D vector styling, balanced margins, generous white space, and no canvas title, subtitle, or footer.

The diagram is arranged horizontally in three main rounded rectangular stages from left to right, aligned along the horizontal center, connected by two thick Charcoal right-pointing arrows, with a single thin rectangular strip running across the bottom.

1. Left stage:
   - A rounded rectangular card with a solid Purple border and a light purple fill.
   - At the top-left inside the box, a minimalist flat vector person silhouette glyph.
   - Inside the box, centered horizontally:
     - Top line in bold Charcoal sans-serif: "A store exception event"
     - Directly below in regular Charcoal sans-serif: "an on-shelf availability exception"
     - Next line below in regular Charcoal sans-serif: "product, store, observation time"

2. Connecting arrow 1:
   - A thick, crisp Charcoal horizontal arrow pointing from the right edge of the left box to the left edge of the middle box.
   - Positioned clearly above the arrow, horizontal regular sans-serif text in Charcoal: "event in"

3. Middle stage:
   - A larger, slightly taller rounded rectangular card with a solid Blue border and a light blue fill.
   - Inside the box, upper portion:
     - Top line in bold Charcoal sans-serif: "An agent with no chat"
     - Second line in regular Charcoal sans-serif: "reads the stock position"
     - Third line in regular Charcoal sans-serif: "forms one supported recommendation"
     - Fourth line in regular Charcoal sans-serif: "structured output, not prose"
   - Inside the box, along the bottom interior row, two distinct small white rounded rectangular chips with solid Blue borders side by side:
     - Left chip text in Charcoal sans-serif: "Pub/Sub toolset"
     - Right chip text in Charcoal monospace font on a single unbroken line: "output_schema"

4. Connecting arrow 2:
   - A thick, crisp Charcoal horizontal arrow pointing from the right edge of the middle box to the left edge of the right box.
   - Positioned clearly above the arrow, horizontal regular sans-serif text in Charcoal: "recommendation out"

5. Right stage:
   - A rounded rectangular card with a solid Green border and a light green fill.
   - Inside the box, centered horizontally:
     - Top line in bold Charcoal sans-serif: "A published recommendation"
     - Directly below in regular Charcoal sans-serif: "on a Pub/Sub topic for a person to review"
     - Next line below in regular Charcoal sans-serif: "no task is created"

6. Bottom strip:
   - Positioned below all three stages with generous vertical spacing, a single thin horizontal bar with a light gray fill and subtle border spanning across the width of the diagram.
   - Centered inside the bar in regular Charcoal sans-serif text: "event in, structured recommendation out; the store manager decides"

Visual rules:
- Ultra-sharp 4K technical documentation graphic, flat 2D layout, high contrast, zero drop shadows, zero gradients, zero 3D effects.
- All text must be horizontal, perfectly legible, and strictly limited to the quoted strings above. No other text, brand names, or logos appear anywhere on the canvas.
