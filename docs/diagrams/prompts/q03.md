# q03

**Slide:** none (quickstart 03 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/03-form-completion-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A damage or loss report', smaller lines: 'an associate describes what happened', 'some fields are missing'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'Collect, validate, confirm', smaller lines: 'ask only for what is missing', 'code validates each field', 'the person confirms before anything is submitted'. Inside the box at the bottom, small White chips with a Blue border in a row: 'get_user_choice', 'require_confirmation'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'The validated record', smaller lines: 'an incident identifier', 'nothing is written without a yes'.

The first arrow is labelled 'describes' and the second 'confirms'.

BOTTOM, one thin Gray strip across the full width: 'slot filling by the model, validation by code, the risky step behind a confirmation'.

Exactly three boxes, 2 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A clean, professional 2D technical process diagram on a solid White 16:9 widescreen canvas, presented without any title, subtitle, header, or footer.

The diagram consists of three horizontally aligned stage boxes arranged from left to right across the middle of the canvas, connected sequentially by two thick horizontal Charcoal arrows, and underscored by a single thin horizontal strip spanning across the bottom.

On the left, there is a rounded rectangle box with a Light Purple fill, a solid Purple border, and a small, minimal flat person silhouette glyph in Purple at its top-left corner. Inside this box, centered horizontally with comfortable padding, is a bold Charcoal headline reading "A damage or loss report", followed below by two smaller, regular Charcoal text lines:
"an associate describes what happened"
"some fields are missing"

To the right of the left box, a thick horizontal Charcoal arrow points rightward toward the middle box. Centered directly above this arrow is the Charcoal text label "describes".

In the center, there is a slightly taller and wider rounded rectangle box with a Light Blue fill and a solid Blue border. Inside this box, centered near the top, is a bold Charcoal headline reading "Collect, validate, confirm", followed below by three smaller, regular Charcoal text lines:
"ask only for what is missing"
"code validates each field"
"the person confirms before anything is submitted"
Positioned horizontally side by side near the bottom interior of this middle box are two small rectangular chips with White fill, solid Blue borders, and rounded corners. The left chip contains the monospace Charcoal text "get_user_choice" on a single unbroken line. The right chip contains the monospace Charcoal text "require_confirmation" on a single unbroken line.

To the right of the middle box, a second thick horizontal Charcoal arrow points rightward toward the right box. Centered directly above this arrow is the Charcoal text label "confirms".

On the right, there is a rounded rectangle box with a Light Green fill and a solid Green border. Inside this box, centered horizontally with comfortable padding, is a bold Charcoal headline reading "The validated record", followed below by two smaller, regular Charcoal text lines:
"an incident identifier"
"nothing is written without a yes"

Below all three boxes, spanning horizontally across the width of the diagram with generous margins, is a single thin horizontal strip with a Light Gray fill and rounded ends. Centered inside this strip is a single line of regular Charcoal text reading:
"slot filling by the model, validation by code, the risky step behind a confirmation"

Visual Style and Constraints:
- Flat 2D vector graphic design with clear visual hierarchy and generous negative space.
- Exactly three main boxes, two internal chips, two connector arrows, and one bottom strip; no other boxes, icons, or decorative elements.
- All text must be horizontal, perfectly legible, and rendered exactly as double-quoted, with no other words, headers, numbers, or labels appearing anywhere in the image.
- Solid color fills and clean borders; no gradients, drop shadows, glossy effects, 3D elements, or photographic textures.
