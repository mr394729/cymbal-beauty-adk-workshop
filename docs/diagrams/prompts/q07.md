# q07

**Slide:** none (quickstart 07 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/07-document-extraction-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A promotion proof', smaller lines: 'a PDF or image of the signage', 'uploaded as an artifact'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'Extract, review, compare', smaller lines: 'the model reads the proof into a structured schema', 'a critic loop reviews the draft and stops itself', "code compares the lines with the week's plan". Inside the box at the bottom, small White chips with a Blue border in a row: 'output_schema', 'LoopAgent', 'exit_loop'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'The discrepancies', smaller lines: 'what the proof says that the plan does not', 'decided by code, not by the model'.

The first arrow is labelled 'uploads' and the second 'reports'.

BOTTOM, one thin Gray strip across the full width: 'multimodal extraction with an output schema; a generate-and-review loop; deterministic checks'.

Exactly three boxes, 3 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A professional, clean, technical architecture diagram in 16:9 landscape format on a pure solid White background. The visual style is modern, 2D flat vector, high contrast, with crisp lines and generous padding, suitable for technical documentation. There is NO canvas title, NO subtitle, and NO footer.

The layout consists of three primary rounded rectangular stage boxes positioned horizontally across the upper two-thirds of the canvas from left to right, connected by two thick Charcoal right-pointing arrows, and one thin horizontal strip spanning across the bottom.

1. Left box:
- Fill is Light Purple with a solid Purple border and rounded corners.
- Located inside at the top-left of the box is a simple, minimalist Charcoal silhouette glyph of a person.
- Level 1 text in bold Charcoal: "A promotion proof"
- Level 2 text directly beneath in regular Charcoal:
  "a PDF or image of the signage"
  "uploaded as an artifact"

2. Connecting arrow 1:
- A thick horizontal Charcoal arrow pointing from the right edge of the left box to the left edge of the middle box.
- Clearly labeled above the arrow in regular Charcoal text: "uploads"

3. Middle box:
- Slightly wider and taller than the side boxes.
- Fill is Light Blue with a solid Blue border and rounded corners.
- Level 1 text in bold Charcoal: "Extract, review, compare"
- Level 2 text directly beneath in regular Charcoal:
  "the model reads the proof into a structured schema"
  "a critic loop reviews the draft and stops itself"
  "code compares the lines with the week's plan"
- Along the bottom inside of this middle box, arranged in a neat horizontal row, are exactly three small rounded rectangular chips with White fill and thin Blue borders. Each chip contains single-line horizontal monospace Charcoal text:
  - First chip: "output_schema"
  - Second chip: "LoopAgent"
  - Third chip: "exit_loop"

4. Connecting arrow 2:
- A thick horizontal Charcoal arrow pointing from the right edge of the middle box to the left edge of the right box.
- Clearly labeled above the arrow in regular Charcoal text: "reports"

5. Right box:
- Fill is Light Green with a solid Green border and rounded corners.
- Level 1 text in bold Charcoal: "The discrepancies"
- Level 2 text directly beneath in regular Charcoal:
  "what the proof says that the plan does not"
  "decided by code, not by the model"

6. Bottom strip:
- Positioned horizontally below the three stage boxes with clear vertical separation.
- A thin, wide rectangular strip with rounded ends or subtle corners, filled with Light Gray and a thin Gray border.
- Centered inside the strip is single-line regular Charcoal text:
  "multimodal extraction with an output schema; a generate-and-review loop; deterministic checks"

All text is rendered horizontally, fully legible, and matches the exact quoted strings. The canvas contains exactly three stage boxes, three internal chips, two labeled arrows, and one bottom strip. There are no other elements, logos, drop shadows, or background graphics.
