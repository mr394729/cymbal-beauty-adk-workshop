# q06

**Slide:** none (quickstart 06 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/06-memory-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A preference', smaller lines: 'huddle time and focus areas', 'said once, weeks ago'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'State now, memory later', smaller lines: 'user state holds validated fields for this session', 'after a turn the session is added to memory', 'a later conversation searches it'. Inside the box at the bottom, small White chips with a Blue border in a row: 'user: state', 'preload_memory', 'Memory Bank'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'A remembered context', smaller lines: 'the assistant already knows the huddle time', 'same code locally and on Agent Runtime'.

The first arrow is labelled 'says' and the second 'recalls'.

BOTTOM, one thin Gray strip across the full width: 'user state and Memory Bank are different things, used for different jobs'.

Exactly three boxes, 3 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A clean, modern technical system architecture diagram in 16:9 landscape format on a solid White background, designed for software documentation. The composition is minimal, sharp, and highly legible, featuring three primary rounded rectangular boxes arranged horizontally in a single row across the center, linked by two thick Charcoal right-pointing arrows, with a single thin horizontal strip positioned beneath them.

There is NO title, NO subtitle, and NO footer on the canvas. Every element sits comfortably within generous outer margins.

Left box:
- Shape: Rounded rectangle with smooth 12px corners, light purple background fill, and a solid Purple border.
- Top-left interior: A small, minimalist generic person silhouette glyph rendered in Purple.
- Text content (horizontal, left-aligned with ample internal padding):
  - Primary line in bold Charcoal: "A preference"
  - Secondary line in regular Charcoal: "huddle time and focus areas"
  - Third line in regular Charcoal: "said once, weeks ago"

First connection:
- A thick, solid Charcoal arrow pointing horizontally from the right edge of the left box to the left edge of the middle box.
- Centered horizontally above the arrow shaft is a clean Charcoal label: "says"

Middle box:
- Shape: Rounded rectangle with smooth 12px corners, noticeably wider than the left and right boxes, light blue background fill, and a solid Blue border.
- Text content (horizontal, left-aligned in the upper section with generous internal padding):
  - Primary line in bold Charcoal: "State now, memory later"
  - Supporting line 1 in regular Charcoal: "user state holds validated fields for this session"
  - Supporting line 2 in regular Charcoal: "after a turn the session is added to memory"
  - Supporting line 3 in regular Charcoal: "a later conversation searches it"
- Bottom interior section: A horizontal row of three distinct small rounded rectangular chips, each featuring a solid White fill, a crisp Blue border, and centered horizontal Charcoal text:
  - First chip: "user: state"
  - Second chip: "preload_memory"
  - Third chip: "Memory Bank"

Second connection:
- A thick, solid Charcoal arrow pointing horizontally from the right edge of the middle box to the left edge of the right box.
- Centered horizontally above the arrow shaft is a clean Charcoal label: "recalls"

Right box:
- Shape: Rounded rectangle with smooth 12px corners, light green background fill, and a solid Green border.
- Text content (horizontal, left-aligned with generous internal padding):
  - Primary line in bold Charcoal: "A remembered context"
  - Secondary line in regular Charcoal: "the assistant already knows the huddle time"
  - Third line in regular Charcoal: "same code locally and on Agent Runtime"

Bottom strip:
- Shape: A single, thin horizontal band stretching across the width beneath the three boxes, with a subtle light Gray fill, a thin medium Gray border, and slightly rounded corners.
- Text content: Centered horizontally inside the strip in regular Charcoal text:
  - "user state and Memory Bank are different things, used for different jobs"

Visual rules and typography:
- Flat 2D vector graphic aesthetic, subtle flat drop shadows under the three main boxes, zero gradients, no 3D bevels, no photorealism.
- High-contrast typography using a clean geometric sans-serif font. All text is perfectly horizontal and completely unrotated.
- Only exactly three main boxes, three interior chips, two connector arrows, and one bottom strip appear. Absolutely no extra shapes, icons, logos, vendor marks, or unquoted text strings.
