# q10

**Slide:** none (quickstart 10 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/10-multi-agent-router/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A question for the team', smaller lines: 'coaching, or a stock check', 'one coordinator receives it'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'Route by description', smaller lines: 'sub-agent descriptions are the routing table', 'coaching: hand the conversation over', 'stock: ask a specialist for one answer'. Inside the box at the bottom, small White chips with a Blue border in a row: 'transfer_to_agent', 'single_turn', 'patterns/'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'Two kinds of result', smaller lines: 'a transfer, or a tool result', 'the events show which one happened'.

The first arrow is labelled 'asks' and the second 'answers'.

BOTTOM, one thin Gray strip across the full width: 'chat mode transfers the conversation; single-turn mode returns one result'.

Exactly three boxes, 3 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A wide 16:9 flat vector technical architecture diagram on a solid White background, designed with clean modern UI aesthetics and generous white space. There is no diagram title, no subtitle, and no footer. The diagram consists of exactly three primary rounded rectangular boxes arranged in a single horizontal row from left to right, connected by two thick Charcoal right-pointing arrows, with a single thin horizontal strip positioned underneath.

On the left, a rounded rectangular box with a Light Purple background fill and a solid Purple border. At the upper-left inside this box is a small, minimal generic silhouette icon of a person. Inside the box, clear horizontal text in Charcoal features a bold title reading "A question for the team", followed below by two lines of smaller regular-weight text reading "coaching, or a stock check" and "one coordinator receives it".

In the center, a slightly wider rounded rectangular box with a Light Blue background fill and a solid Blue border. Centered inside, clear horizontal text in Charcoal features a bold title reading "Route by description", followed below by three lines of smaller regular-weight text: "sub-agent descriptions are the routing table", "coaching: hand the conversation over", and "stock: ask a specialist for one answer". Positioned horizontally in a row near the bottom inside this middle box are three small White rectangular pill chips with thin Blue borders, each displaying clean horizontal text: the first chip reads "transfer_to_agent", the second chip reads "single_turn", and the third chip reads "patterns/".

On the right, a rounded rectangular box with a Light Green background fill and a solid Green border. Inside the box, clear horizontal text in Charcoal features a bold title reading "Two kinds of result", followed below by two lines of smaller regular-weight text reading "a transfer, or a tool result" and "the events show which one happened".

Connecting the left box to the middle box is a thick, solid Charcoal horizontal arrow pointing to the right, with a clear horizontal label centered directly above the arrow reading "asks".

Connecting the middle box to the right box is a thick, solid Charcoal horizontal arrow pointing to the right, with a clear horizontal label centered directly above the arrow reading "answers".

Beneath all three boxes, centered horizontally with generous padding, is a single thin Light Gray horizontal strip with a subtle border spanning across the width of the canvas. Inside the strip, centered horizontal Charcoal text reads: "chat mode transfers the conversation; single-turn mode returns one result".

All text is crisp, strictly horizontal, perfectly legible, and rendered in a clean geometric sans-serif typeface, with monospace styling for the three chips. Flat 2D vector styling with subtle soft drop shadows, consistent line weights, no gradients, no photorealism, and ample margin space around all edges.
