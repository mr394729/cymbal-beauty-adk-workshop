# q02

**Slide:** none (quickstart 02 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/02-rag-knowledge-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A procedure question', smaller lines: 'pickup holds, signage, cycle counts', 'locked-case handling'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'Search the store procedures', smaller lines: 'one retrieval tool over an indexed document store', 'the passages come back with their source'. Inside the box at the bottom, small White chips with a Blue border in a row: 'policy_lookup', 'Vertex AI Search'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'A grounded answer', smaller lines: 'names the procedure and the source', 'says when nothing covers it'.

The first arrow is labelled 'asks' and the second 'answers with a source'.

BOTTOM, one thin Gray strip across the full width: 'the same policy_lookup tool serves this agent and the store manager agent'.

Exactly three boxes, 2 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A professional 2D technical process architecture diagram on a pure solid white background, in a 16:9 landscape aspect ratio. The diagram has no canvas title, no subtitle, and no footer. The composition consists of exactly three rounded rectangular boxes arranged horizontally left to right, connected by two thick Charcoal right-pointing arrows, with one thin horizontal strip positioned beneath them.

The left box has a light purple fill with a solid Purple border and rounded corners. In the top-left corner inside the box is a small, minimalist Charcoal person silhouette glyph. Inside the box, centered horizontally, the top line is bold Charcoal text: "A procedure question". Directly below it in a smaller, regular weight Charcoal font are two lines of text: "pickup holds, signage, cycle counts" followed by "locked-case handling".

To the right of the left box is the center box, which is slightly wider. It has a light blue fill with a solid Blue border and rounded corners. Centered inside near the top is bold Charcoal text: "Search the store procedures". Directly below it in a smaller regular Charcoal font are two lines: "one retrieval tool over an indexed document store" followed by "the passages come back with their source". At the bottom inside this box sits a horizontal row of exactly two small pill-shaped chips with white fill and a solid Blue border. The first chip contains the monospace text "policy_lookup" and the second chip contains the monospace text "Vertex AI Search".

To the right of the center box is the right box. It has a light green fill with a solid Green border and rounded corners. Centered inside near the top is bold Charcoal text: "A grounded answer". Directly below it in a smaller regular Charcoal font are two lines: "names the procedure and the source" followed by "says when nothing covers it".

Connecting the left box to the middle box is a single thick straight Charcoal arrow pointing to the right, labeled neatly above its shaft with the regular Charcoal text "asks". Connecting the middle box to the right box is another single thick straight Charcoal arrow pointing to the right, labeled neatly above its shaft with the regular Charcoal text "answers with a source".

Running horizontally across the canvas beneath all three boxes is a single thin rounded strip with a light gray fill and a subtle Gray border. Centered along this strip is regular Charcoal text: "the same policy_lookup tool serves this agent and the store manager agent".

All text is strictly horizontal, perfectly legible, with generous white space and margins throughout. Clean flat vector graphic style with crisp lines, no 3D effects, no harsh drop shadows, and no decorative clutter.
