# q08

**Slide:** none (quickstart 08 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/08-mcp-tools-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A stock question', smaller lines: 'from a signed-in associate', 'for their own store'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'Tools the agent never imports', smaller lines: 'McpToolset discovers the tools from a server', 'a callback stamps the signed-in store on every call', 'the server validates the store it was given'. Inside the box at the bottom, small White chips with a Blue border in a row: 'McpToolset', 'stdio server'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'The stock facts', smaller lines: 'from shared functions behind the server', 'swap the server, keep the agent'.

The first arrow is labelled 'asks' and the second 'answers'.

BOTTOM, one thin Gray strip across the full width: "the agent is an MCP client; the store scope stays on the agent's side".

Exactly three boxes, 2 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A clean, flat, 16:9 technical architecture diagram for developer documentation, rendered in modern vector slide style on a pure White background. The canvas has generous white space, balanced composition, no diagram title, no subtitle, and no footer. Every label is strictly horizontal, large, crisp, and legible.

In the upper two-thirds of the canvas, three rounded rectangle boxes are arranged in a horizontal row from left to right, linked sequentially by two thick horizontal Charcoal arrows pointing to the right.

On the left, a rounded rectangular box with a Light Purple fill and a solid Purple border. In the upper-left corner inside this box is a simple minimalist person silhouette glyph. Inside the box, centered text features a bold header reading "A stock question", followed directly below by two smaller regular lines reading "from a signed-in associate" and "for their own store".

To the right of the left box, a thick Charcoal arrow points horizontally toward the middle box, labeled above with the regular Charcoal text "asks".

In the center, a slightly larger rounded rectangular box with a Light Blue fill and a solid Blue border. Centered text inside features a bold header reading "Tools the agent never imports", followed directly below by three smaller regular lines reading "McpToolset discovers the tools from a server", "a callback stamps the signed-in store on every call", and "the server validates the store it was given". Below these lines, along the bottom interior of the box, is a horizontal row of two separate small rounded White chips with thin Blue borders, reading "McpToolset" on the first chip and "stdio server" on the second chip.

To the right of the middle box, a thick Charcoal arrow points horizontally toward the right box, labeled above with the regular Charcoal text "answers".

On the right, a rounded rectangular box with a Light Green fill and a solid Green border. Centered text inside features a bold header reading "The stock facts", followed directly below by two smaller regular lines reading "from shared functions behind the server" and "swap the server, keep the agent".

Spanning horizontally across the bottom of the canvas beneath the three boxes is a single, thin rounded horizontal strip with a Light Gray fill and subtle border. Centered horizontally inside this strip is Charcoal text reading "the agent is an MCP client; the store scope stays on the agent's side".

Visual execution is clean and flat with no 3D effects, no gradients, no drop shadows, and no logos or trademarks. The composition contains exactly three main boxes, two chips inside the middle box, two arrows, and one bottom strip. All text is perfectly horizontal, properly padded, and matches the quoted strings exactly.
