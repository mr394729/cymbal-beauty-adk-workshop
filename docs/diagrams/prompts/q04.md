# q04

**Slide:** none (quickstart 04 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/04-external-api-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A question about an order', smaller lines: 'status and pickup hold time', 'or the local weather'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'Tools from an API description', smaller lines: 'an OpenAPI document becomes tools', 'one plain function calls a public weather API', 'the key is a secret reference, not code'. Inside the box at the bottom, small White chips with a Blue border in a row: 'OpenAPIToolset', 'Secret Manager'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'An answer from the service', smaller lines: 'the returned fields, summarised', 'bounded retries on a correctable error'.

The first arrow is labelled 'asks' and the second 'answers'.

BOTTOM, one thin Gray strip across the full width: 'generate tools from an OpenAPI spec instead of hand-writing wrappers'.

Exactly three boxes, 2 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A high-resolution 16:9 horizontal technical diagram in a clean, modern flat vector style on a solid White background. The composition consists of exactly three horizontal stages placed side by side, connected by two thick horizontal arrows, with a single thin horizontal strip running underneath. The diagram contains no overall title, no subtitle, and no footer.

The three primary boxes are vertically centered and spaced evenly from left to right across the canvas with generous margins:

1. The leftmost component is a wide rounded rectangle with a Light Purple fill and a solid Purple border. In the upper left corner inside the box is a small, minimalist Charcoal silhouette icon of a person. Inside the box, centered horizontally in dark Charcoal text, the bold header reads "A question about an order". Below this header, two smaller regular-weight lines read:
"status and pickup hold time"
"or the local weather"

2. Connecting the leftmost box to the middle box is a thick, solid Charcoal horizontal arrow pointing directly to the right. Centered directly above this arrow is the regular Charcoal text label "asks".

3. The middle component is a larger, slightly wider rounded rectangle with a Light Blue fill and a solid Blue border. Centered horizontally near the top in dark Charcoal text, the bold header reads "Tools from an API description". Directly below the header, three smaller regular-weight lines read:
"an OpenAPI document becomes tools"
"one plain function calls a public weather API"
"the key is a secret reference, not code"
Positioned horizontally in a row near the bottom inside this box are two small rectangular pill-shaped chips with a White fill and a solid Blue border. The first chip contains the Charcoal monospace text "OpenAPIToolset". The second chip contains the Charcoal monospace text "Secret Manager".

4. Connecting the middle box to the rightmost box is a thick, solid Charcoal horizontal arrow pointing directly to the right. Centered directly above this arrow is the regular Charcoal text label "answers".

5. The rightmost component is a wide rounded rectangle with a Light Green fill and a solid Green border. Inside the box, centered horizontally in dark Charcoal text, the bold header reads "An answer from the service". Below this header, two smaller regular-weight lines read:
"the returned fields, summarised"
"bounded retries on a correctable error"

Below these three stages, spanning across the full width of the diagram area with comfortable margins, sits a single thin horizontal strip with a Light Gray fill and a subtle Gray border. Centered horizontally within this strip is regular-weight Charcoal text reading:
"generate tools from an OpenAPI spec instead of hand-writing wrappers"

All text is strictly horizontal, rendered in a crisp geometric sans-serif typeface, with clear visual hierarchy, ample white space around every element, and no text wrapping or overlapping. There are no other components, logos, icons, or text on the canvas.
