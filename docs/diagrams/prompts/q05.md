# q05

**Slide:** none (quickstart 05 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/05-data-analyst-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name 'A data question', smaller lines: 'trends, forecasts, anomalies', 'over the store records'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'Governed analytics tools', smaller lines: 'read-only SQL with row and byte limits', 'insights, forecast and anomaly tools', 'a callback keeps people data out'. Inside the box at the bottom, small White chips with a Blue border in a row: 'BigQueryToolset', 'ask_data_insights', 'forecast'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name 'A figure and its query', smaller lines: 'the number, the SQL that produced it', 'the limits that applied'.

The first arrow is labelled 'asks' and the second 'answers'.

BOTTOM, one thin Gray strip across the full width: 'analytical access to a dataset by configuration, not code'.

Exactly three boxes, 3 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A clean, professional 2D software architecture diagram on a solid pure White background with a 16:9 landscape aspect ratio. Flat modern technical documentation vector style, crisp sharp outlines, generous whitespace, with no 3D elements, no gradients, and no drop shadows.

The diagram contains exactly three horizontal stages placed from left to right across the middle of the canvas, connected by exactly two straight horizontal Charcoal arrows, and a single thin Gray horizontal strip running across the bottom. There is no diagram title, no subtitle, and no footer.

On the left side:
A rounded rectangle box with a soft light purple fill and a solid Purple border. In the upper-left corner inside the box is a small, minimalist generic person silhouette glyph in Purple. Inside the box, centered horizontally below the glyph, is bold Charcoal text:
"A data question"
Directly underneath in smaller, regular-weight Charcoal text are two separate lines:
"trends, forecasts, anomalies"
"over the store records"

Connecting the left box to the middle box:
A thick, solid Charcoal arrow pointing horizontally to the right. Centered directly above the arrow shaft is a horizontal text label in regular Charcoal font:
"asks"

In the center:
A larger rounded rectangle box with a soft light blue fill and a solid Blue border. Centered at the top inside this box is bold Charcoal text:
"Governed analytics tools"
Directly underneath in smaller, regular-weight Charcoal text are three separate lines:
"read-only SQL with row and byte limits"
"insights, forecast and anomaly tools"
"a callback keeps people data out"
Along the bottom inside this middle box is a single horizontal row of three distinct rounded pill-shaped chips with solid White backgrounds and thin Blue borders. Each chip contains horizontal text in Charcoal:
The first chip reads "BigQueryToolset"
The second chip reads "ask_data_insights"
The third chip reads "forecast"

Connecting the middle box to the right box:
A thick, solid Charcoal arrow pointing horizontally to the right. Centered directly above the arrow shaft is a horizontal text label in regular Charcoal font:
"answers"

On the right side:
A rounded rectangle box with a soft light green fill and a solid Green border, identical in height to the left box. Inside the box, centered horizontally, is bold Charcoal text:
"A figure and its query"
Directly underneath in smaller, regular-weight Charcoal text are two separate lines:
"the number, the SQL that produced it"
"the limits that applied"

Across the bottom:
Spanning horizontally across the width of the canvas, positioned well below the three main boxes with clear breathing room, is a single thin horizontal strip with a soft light gray fill and a subtle Gray border. Centered horizontally inside this strip is a single line of regular Charcoal text:
"analytical access to a dataset by configuration, not code"

Typography is clean, highly legible modern geometric sans-serif. All text is strictly horizontal with ample inner padding so that no words break across lines and identifiers with underscores remain unbroken on one line. No brand logos, no vendor marks, and no text exists on the canvas other than the exact quoted strings specified.
