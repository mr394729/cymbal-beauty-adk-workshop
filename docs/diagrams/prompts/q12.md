# q12

**Slide:** none (quickstart 12 README and walkthrough; docs/diagrams/README.md)

**Aspect:** 16:9

**Pipeline:** gemini-diagramming. Step 1 `gemini-3.8-flash` (thinking HIGH) crafts the image prompt from the description below; Step 2 `gemini-3.1-flash-image` renders 16:9 at `image_size=4K` with thinking off. Creative direction: `_creative_direction.md`. The page supplies the title and the copy, so the image carries no title.

**Source of truth:** `quickstarts/12-a2a-agent/agent.py`, its README

## Description (Step 1 input — edit this to regenerate)

The canvas is 16:9. This image sits under a heading on a documentation page, so the image has NO title, NO subtitle and NO footer. Fewer, larger elements; every label must stay legible when the image is shown about 900 px wide.

TWO TEXT LEVELS: a box has a bold name and, where given, one or two smaller regular lines under it.

Render this as THREE STAGES left to right, joined by exactly two thick Charcoal right-pointing arrows, with one thin strip under them.

LEFT STAGE, one rounded box with a light purple fill and a Purple border and a person glyph at its top left. Bold name "Another team's assistant", smaller lines: 'a regional question that needs store detail', 'knows only a card URL'.

MIDDLE STAGE, one larger rounded box with a light blue fill and a Blue border. Bold name 'Delegate over Agent2Agent', smaller lines: "the remote agent's card says what it can do", 'RemoteA2aAgent sends the request', 'the store app answers as a service'. Inside the box at the bottom, small White chips with a Blue border in a row: 'agent card', 'RemoteA2aAgent', 'Agent Registry'.

RIGHT STAGE, one rounded box with a light green fill and a Green border. Bold name "The store's answer", smaller lines: 'returned to the regional assistant', 'two teams, two deployments, one protocol'.

The first arrow is labelled 'asks' and the second 'answers'.

BOTTOM, one thin Gray strip across the full width: 'expose an ADK app over A2A with one flag and an agent card; consume it with RemoteA2aAgent'.

Exactly three boxes, 3 chips, two arrows and one strip. No other components.

The only text on the canvas is the quoted labels above. Every box sits fully inside the canvas with a clear margin; nothing is cropped by the edge. Generous padding; arrows route around boxes; all labels horizontal. Every box is wide enough that its label stays on one line and an identifier never breaks at an underscore.
TEXT AND COLOR GUARD: the picture contains no text except the double-quoted strings in this description, and no retailer or vendor name or logo. Words written in capitals here (TOP, MIDDLE, BOTTOM, LEFT, RIGHT, CENTER, ROW, COLUMN, LANE and the like) are layout instructions and never appear in the picture. In your prompt, name colors only by word (Blue, Yellow, Green, Red, Purple, Orange, Gray, Charcoal) and never write a hex code; describe positions in lowercase prose and never give a region, row, bar or legend a name of its own. A region with no quoted title has no title.
## Crafted image prompt (Step 1 output used for the current render)

A technical architecture flow diagram in a clean, modern flat style on a solid White 16:9 landscape canvas. The diagram contains no overall title, no subtitle, and no footer.

In the upper two-thirds of the canvas, three rounded rectangular boxes are arranged in a horizontal row from left to right, evenly spaced with generous padding, connected sequentially by two thick horizontal Charcoal arrows pointing from left to right.

The left box has a Light Purple fill with a solid Purple border. Inside the box at the top left is a simple, minimalist Purple silhouette glyph of a person. Inside the left box, all text is horizontal and centered: at the top is the bold Charcoal text "Another team's assistant", followed below by two lines of smaller regular Charcoal text: "a regional question that needs store detail" and "knows only a card URL".

Between the left box and the middle box is a thick horizontal Charcoal arrow pointing right toward the middle box, clearly labeled above the shaft with the Charcoal regular text "asks".

The middle box is slightly larger than the left and right boxes, featuring a Light Blue fill with a solid Blue border. Inside the middle box, all text is horizontal and centered: at the top is the bold Charcoal text "Delegate over Agent2Agent", followed below by three lines of smaller regular Charcoal text:
"the remote agent's card says what it can do"
"RemoteA2aAgent sends the request"
"the store app answers as a service"
At the bottom interior of this middle box is a horizontal row of exactly three small, pill-shaped rounded chips with White fill, solid Blue borders, and Charcoal monospace text: the first chip reads "agent card", the second chip reads "RemoteA2aAgent", and the third chip reads "Agent Registry".

Between the middle box and the right box is a thick horizontal Charcoal arrow pointing right toward the right box, clearly labeled above the shaft with the Charcoal regular text "answers".

The right box has a Light Green fill with a solid Green border. Inside the right box, all text is horizontal and centered: at the top is the bold Charcoal text "The store's answer", followed below by two lines of smaller regular Charcoal text: "returned to the regional assistant" and "two teams, two deployments, one protocol".

Positioned horizontally across the bottom of the canvas, separated by clear vertical margin from the three upper boxes, is a single thin horizontal strip with a Light Gray fill and a subtle Gray border, stretching across the width of the diagram. Centered horizontally inside this strip is a single line of regular Charcoal text: "expose an ADK app over A2A with one flag and an agent card; consume it with RemoteA2aAgent".

All typography is rendered in a crisp, legible geometric sans-serif typeface, sized large enough for documentation slides. Boxes have generous internal padding and rounded corners. There are no shadows, no gradients, no 3D effects, and no logos or watermarks. The only text visible on the image are the explicitly quoted strings.
